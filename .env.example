# Configuration de ce checkout pour docker-compose (PAS backend/.env).
# Copiez vers .env et adaptez selon l'environnement de ce checkout :
#   - production (branche main)    -> BACKEND_PORT=3001
#   - staging (branche staging)    -> BACKEND_PORT=3002
BACKEND_PORT=3001
